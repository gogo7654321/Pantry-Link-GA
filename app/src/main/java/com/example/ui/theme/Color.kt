package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Calming, organic hospitable green (Primary)
val OrganicGreenPrimary = Color(0xFF2C5E43) // Warm forest green
val OrganicGreenLight = Color(0xFFF4FAF6) // Warm light sage tea tint
val OrganicGreenContainer = Color(0xFFD3E7DD) // Soft tea container

// Warm indicators / Highlights (Soft Greens & Terracotta Peach)
val EmeraldMedium = Color(0xFF3B855F) // Soft medium forest green
val EmeraldGreen600 = Color(0xFF459A6F)
val EmeraldGreen500 = Color(0xFF55AF80)
val EmeraldGreen400 = Color(0xFF7CC5A1)

// Muted Warm Charcoal & Sage-Gray (Secondary)
val MutedSlateBlue = Color(0xFF536A5F) // Soft charcoal sage-gray
val MutedSlateBlueLight = Color(0xFFFDFBF8) // Real warm paper tint
val MutedSlateBlueContainer = Color(0xFFF0EBE3) // Cozy oatmeal-beige container

// Cozy Terracotta sand / Peach Sunset (Warm Accent / Tertiary)
val WarmBeige = Color(0xFFD47A5C) // Terracotta Peach
val WarmBeigeContainer = Color(0xFFF6E7DF) // Terracotta sand container

// Human & Hospitable Background / Surfaces
val CalmBackground = Color(0xFFFAF8F5) // Soft oatmeal cream base paper
val SoftSurface = Color(0xFFFFFFFF) // Crisp white with organic contrast
val TextDark = Color(0xFF1E2622) // Soft deep moss charcoal (infinitely softer than plain black/slate)
val TextMuted = Color(0xFF5A6961) // Warm mossy sage muted text

// Cozy Dark Theme counterparts of Hospitable Theme
val OrganicGreenDark = Color(0xFF88D2AC) // Bright comforting sage green
val MutedSlateBlueDark = Color(0xFF8DA397) // Warm sage charcoal dark text
val WarmBeigeDark = Color(0xFFECA38A) // Glowing terracotta warm sunset
val DarkBackground = Color(0xFF111815) // Deep pine forest dark background (not flat pitch black)
val DarkSurface = Color(0xFF1A2621) // Cozy warm dark forest surface card
val TextLight = Color(0xFFF0F5F2) // Off-white cream text
val TextLightMuted = Color(0xFFA2B6AC) // Muted warm sage text

